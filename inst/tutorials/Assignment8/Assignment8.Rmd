---
title: "Статистический вывод с зависимыми выборками и категориальными переменными"
output: 
  learnr::tutorial:
    language: ru.json
runtime: shiny_prerendered
---

```{r setup-locale, include=FALSE}
Sys.setenv(LANGUAGE = "ru_RU.UTF-8")
Sys.setlocale("LC_ALL", "Russian")
```

```{r setup, include=FALSE}
library(learnr)
library(gradethis)
library(tutorial.helpers)
library(tidyverse)
library(ggstatsplot)
knitr::opts_chunk$set(echo=FALSE)
library(datarium)
library(rstatix)
data("performance")
data<-performance
civil_society<-haven::read_sav("data/civil_society.sav")
civil_society<-haven::as_factor(civil_society)
```


```{r info-section, child = "child_documents/info_section.Rmd"}
```

## Предварительные тестовые задания

### Вопрос 1

```{r q1}
 question_radio("Когда предпочтительнее использовать критерий Уилкоксона для зависимых выборок вместо парного t-теста?",
    answer("Когда сравниваются две независимые группы по количественной переменной"),
    answer("Когда зависимая переменная изначально номинативная с несколькими категориями"),
    answer("Когда распределение разностей между парами существенно отклоняется от нормального и присутствуют сильные выбросы", 
           correct = TRUE),
    answer("Когда выборки чрезвычайно большие (более 10 000 наблюдений), и t-тест становится вычислительно невозможен"),
    incorrect = "Критерий Уилкоксона предпочтителен при нарушениях нормальности разностей пар наблюдений и наличии выбросов. Парный t-тест требует нормального распределения разностей $\\Delta_i = X_i - Y_i$. В R проверяем: `shapiro.test(x-y)` или `qqnorm(x-y)`.",
    allow_retry = TRUE,
    random_answer_order = TRUE
  )
```

### Вопрос 2

```{r q2}
question_radio("В каком случае наиболее уместно применять парный t-критерий Стьюдента для зависимых выборок?",
    answer("Сравнение доли пациентов, вылечившихся в двух разных больницах"),
    answer("Сравнение среднего уровня тревожности у одних и тех же людей до и после тренинга", correct = TRUE),
    answer("Сравнение среднего роста у мужчин и женщин в независимых группах"),
    answer("Сравнение трёх экспериментальных групп по уровню мотивации"),
    incorrect = "Парный t-тест для одних и тех же объектов в двух условиях: $H_0: \\mu_D = 0$, где $D_i = X_i - Y_i$. В R: `t.test(before, after, paired=TRUE)`. Требует нормальности разностей.",
    allow_retry = TRUE
  )
```

### Вопрос 3

```{r q3}
question_radio("Какая ключевая особенность отличает дисперсионный анализ с повторными измерениями (repeated measures ANOVA) от обычного однофакторного ANOVA?",
  answer("Применение только к номинативным факторам с двумя уровнями"),
  answer("Наличие одной группы испытуемых, измеренных несколько раз при разных условиях", correct = TRUE),
  answer("Использование категориальной зависимой переменной вместо количественной"),
  answer("Отсутствие предположения о нормальности распределения данных"),
  incorrect = "Repeated measures ANOVA анализирует $\\textit{одну группу субъектов}$, измеренных $k\\geq2$ раз (within-subject design). Обычный ANOVA — $\\textit{независимые группы}$. В R: `aov(value ~ time + Error(subject/time))`. Требует проверки сферичности: равенство ковариаций $Cov(D_i,D_j)$ для $i\\neq j$.",
  allow_retry = TRUE,
  random_answer_order = TRUE
)
```

### Вопрос 4

```{r q4}
question_radio("Какое утверждение наилучше описывает область применения критерия Фридмана?",
  answer("Сравнение долей успеха между двумя связанными бинарными измерениями"),
  answer("Сравнение двух связанных выборок по количественной переменной в предположении нормальности"),
  answer("Сравнение трёх и более независимых групп по количественной переменной при нарушении нормальности"),
  answer("Сравнение трёх и более связанных выборок по порядковой или не нормально распределённой количественной переменной", 
         correct = TRUE),
  incorrect = "Критерий Фридмана — непараметрический тест для $\\textit{k\\geq3}$ $\\textit{связанных выборок}$ ранговых/порядковых данных. $H_0:$ все $k$ распределений идентичны. Статистика $Q=\\frac{12}{nk(k+1)}\\sum R_j^2-3n(k+1)\\sim\\chi^2_{k-1}$. В R: `friedman.test(y1, y2, y3)`. Альтернатива ANOVA repeated measures при выбросах.",
  allow_retry = TRUE,
  random_answer_order = TRUE
)
```

### Вопрос 5

```{r q5}
question_radio("Для проверки согласованности распределения частот по нескольким категориям при зависимых измерениях (например, повторная оценка тех же людей по номинальной шкале с более чем двумя категориями) какой тест чаще всего применяют?",
  answer("Тест МакНемара"),
  answer("Классический критерий $\\chi^2$ Пирсона для независимых выборок"),
  answer("Критерий Фридмана"),
  answer("Обобщенный критерий $\\chi^2$ для таблиц сопряженности, учитывающий зависимость наблюдений", correct = TRUE),
  incorrect = "Для $\\textit{k>2}$ категорий при связанных наблюдениях используют обобщённый $\\chi^2$ тест для таблиц сопряжённости (marginal homogeneity test). В R: `stuart.maxwell.test(table)` из пакета `sjstats` или `MCN.test()` из `rtest`. Тест МакНемара — только для двух категорий ($2\\times2$). Тест Фридмана больше подходит для ранговых данных.",
  allow_retry = TRUE,
  random_answer_order = TRUE
)

```

### Вопрос 6

```{r q6, warning=FALSE}
question_radio("В каком типичном сценарии применяется тест МакНемара?",
  answer("Сравнение уровня депрессии (по количественной шкале) у пациентов до и после терапии"),
  answer("Сравнение трёх и более условий при бинарной зависимой переменной у тех же людей"),
  answer("Оценка изменения доли курящих среди одних и тех же людей до и после антитабачной кампании", 
         correct = TRUE),
  answer("Сравнение частоты заболевания в двух независимых популяциях"),
  incorrect = "Тест МакНемара для $\\textit{2 связанных бинарных измерений}$ (2×2 таблица сопряжённости). $H_0: p_{12}=p_{21}$, где $b,c$ — несогласованные случаи. Статистика $\\chi^2 = \\frac{(b-c)^2}{b+c}$. В R: `mcnemar.test(table(c(30,15,10,45)))`. Для k≥3 — Кохран Q.",
  allow_retry = TRUE,
  random_answer_order = TRUE
)
```

### Вопрос 7

```{r q7}
question_radio("Какой тест является естественным обобщением теста МакНемара на случай трёх и более зависимых условий при бинарной переменной?",
  answer("Q-критерий Кохрана", correct = TRUE),
  answer("Парный t-тест"),
  answer("Классический $\\chi^2$ Пирсона для независимых выборок"),
  answer("Критерий Фридмана"),
  incorrect = "Q-критерий Кохрана (Cochran's Q) — прямое обобщение МакНемара для $k\\geq3$ связанных бинарных измерений. $H_0:$ одинаковые пропорции успеха. Формула $Q = \\frac{k(n-1)}{n(k-1)} \\cdot \\frac{\\sum (r_i - \\bar{r})^2}{\\bar{r}(1-\\bar{r})} \\sim \\chi^2_{k-1}$. В R: `cochranQTest()` из пакета `clm`. МакНемар — частный случай при $k=2$.",
  allow_retry = TRUE,
  random_answer_order = TRUE
)
```

### Вопрос 8

```{r q8}
question_radio("Какую основную гипотезу проверяет Q-критерий Кохрана при анализе бинарных зависимых данных в нескольких условиях?",
  answer("Что вероятность «успеха» одинакова во всех условиях", correct = TRUE),
  answer("Что распределение разностей между парами симметрично относительно нуля"),
  answer("Что медианы значений во всех условиях равны"),
  answer("Что дисперсии во всех условиях одинаковы"),
  incorrect = "Q-критерий Кохрана проверяет $H_0: p_1 = p_2 = \\dots = p_k$, где $p_j$ — пропорция успехов в $j$-м условии для одних и тех же $n$ субъектов. Статистика $Q = \\frac{k(n-1)}{n(k-1)} \\cdot \\frac{\\sum(r_i-\\bar{r})^2}{\\bar{r}(1-\\bar{r})} \\sim \\chi^2_{k-1}$. В R: `cochranQTest()` из пакета `clm`.",
  allow_retry = TRUE,
  random_answer_order = TRUE
)

```

### Вопрос 9

```{r q9, warning=FALSE}
question_radio("Какой из перечисленных методов лучше всего подходит для сравнения трёх временных точек (до лечения, сразу после, через месяц) по порядковой шкале социофобии у одних и тех же пациентов, при явном нарушении нормальности?",
  answer("Критерий Уилкоксона для независимых выборок"),
  answer("Парный t-тест с поправкой Бонферрони"),
  answer("Критерий Фридмана", correct = TRUE),
  answer("Repeated measures ANOVA"),
  incorrect = "Критерий Фридмана — непараметрический аналог ANOVA repeated measures для $\\textit{k\\geq3}$ связанных выборок порядковых/количественных данных при нарушении нормальности. Работает с рангами внутри каждого субъекта: $Q=\\frac{12}{nk(k+1)}\\sum R_j^2-3n(k+1)\\sim\\chi^2_{k-1}$. В R: `friedman.test(before, after, month)`. ANOVA требует нормальности и сферичности.",
  allow_retry = TRUE,
  random_answer_order = TRUE
)


```

### Вопрос 10

```{r q10}
question_radio("Какой из указанных тестов уместнее всего использовать для оценки изменения распределения категориальной переменной с двумя уровнями (например, «согласен / не согласен») у одних и тех же респондентов в двух временных точках?",
  answer("Критерий $\\chi^2$ Пирсона для независимых выборок"),
  answer("Q-критерий Кохрана"),
  answer("Парный t-тест"),
  answer("Тест МакНемара", correct = TRUE),
  incorrect = "Тест МакНемара специально для $\\textit{2\\times2}$ таблицы сопряжённости при $\\textit{2 связанных бинарных измерениях}$. $H_0: p_{12}=p_{21}$ (симметрия несогласующихся пар). Статистика $\\chi^2=\\frac{(b-c)^2}{b+c}$, где $b,c$ — off-diagonal элементы. В R: `mcnemar.test(table(c(a,b,c,d)))`. Кохран — для $k\\geq3$.",
  allow_retry = TRUE,
  random_answer_order = TRUE
)

```

### Вопрос 11

```{r q11, warning=FALSE}
question_radio("На каком положении основывается тест МакНемара?",
  answer("маргинальные вероятности для каждого исхода равны", correct = TRUE),
  answer("общая доля 'успеха' одинакова в обеих выборках"),
  answer("распределение совместных частот симметрично"),
  answer("ковариация между измерениями равна нулю"),
  incorrect = "Тест МакНемара проверяет $H_0:$ маргинальные вероятности равны, т.е. $p_{\\text{да-нет}} = p_{\\text{нет-да}}$ ($b=c$ в таблице $\\begin{matrix} a & b \\\\ c & d \\end{matrix}$). Это эквивалентно симметрии off-diagonal ячеек. Статистика $\\chi^2 = \\frac{(b-c)^2}{b+c}$.",
  allow_retry = TRUE,
  random_answer_order = TRUE
)
```

### Вопрос 12

```{r q12, warning=FALSE}
question_radio("Что такое 'блоки' в Q-критерии Кохрана?",
  answer("Независимые группы испытуемых", message = "Нет!"),
  answer("Наборы связанных наблюдений: одни и те же субъекты/единицы в k условиях", correct = TRUE),
  answer("Различные экспериментальные воздействия", message = "Это столбцы!"),
  answer("Временные точки измерения", message = "Частный случай блоков."),
  incorrect = "Блоки (b) — $\\textit{связанные наборы}$: n субъектов, каждый проходит k воздействий. Матрица b×k: строки=блоки (субъекты), столбцы=k условий. $X_{ij}=0/1$. $H_0:$ одинаковые пропорции по столбцам. В R: каждая строка — один блок/субъект.",
  allow_retry = TRUE,
  random_answer_order = TRUE
)
```
### Вопрос 13
```{r q13, warning=FALSE}
question_radio("Для отчета размера эффекта в парном t-тесте при n=20 участников эксперимента (до / после участия в серии тренингов на soft skills) какой показатель предпочтительнее указать в статье?",
  answer("Cohen's d = 0.85 (средний эффект)"),
  answer("Hedges' g = 0.81 (с поправкой на малую выборку)", correct = TRUE),
  answer("r = 0.32 (корреляция разностей)"),
  answer("$\\eta^2$ = 0.42 (псевдо $R^2$ из ANOVA)"),
  answer("Ничего — только p-value достаточно"),
  incorrect = "Hedges' g **корректирует bias** Cohen's d при n<50: $g=d\\times(1-\\frac{3}{4n-9})$. При n=20 поправка ~6%! 

**Пример данных:**
```r
before <- c(7,6,8,5,9); after <- c(4,5,3,6,4)
cohens_d(before, after, paired=TRUE)  # d=1.15
hedges_g(before, after, paired=TRUE)  # g=1.08```",
allow_retry = TRUE
)
```
### Вопрос 14
```{r q14, warning=FALSE}
question_radio("В выводе `wilcox.test(before, after, paired=TRUE)` что означает пометка 'with continuity correction' (с поправкой на непрерывность)",
  answer("Точный тест перебором всех перестановок"),
  answer("Добавлена поправка 0.5 к ранговой статистике W для нормального приближения", correct = TRUE),
  answer("Коррекция множественных сравнений Бонферрони"),
  answer("Учет связей методом Pratt (ties='pratt')"),
  answer("Автоматическая проверка нормальности разностей"),
allow_retry = TRUE
)
```
### Вопрос 15
```{r, q15, warning=FALSE}
question_radio("В однофакторном ANOVA (3 группы: уровень образования родителей) какой тип SS выбрать для анализа влияния на политические предпочтения?",
  answer("Type I — стандартный `anova()`", correct = TRUE),
  answer("Type II — `Anova(type=2)`"),
  answer("Type III — только при contr.sum"),
  answer("Не важно — результаты идентичны", correct = TRUE),
  incorrect = "**Однофакторный ANOVA:** Type I=II=III! Только один фактор (образование) → нет взаимодействий для контроля. 

**Пример кода:**
```r
# Данные: полит. предпочтения по образованию родителей
educ <- gl(3, 50, labels=c('среднее', 'высшее1', 'высшее2'))  
pref <- c(rnorm(50,2.1), rnorm(50,3.2), rnorm(50,4.1))  

anova(lm(pref ~ educ))      # Type I ✓
Anova(lm(pref ~ educ),2)    # Type II ✓ (то же!)```",
allow_retry = TRUE
)
```

## Практические задания


### Упражнение 1


С участниками эксперимента два раза провели тест производительности - до и после участия в акселерационной программе. Результаты эксперимента содержатся в наборе `data`. 

Всего в таблице 5 переменных;

 - id (идентификационный номер)
 - gender (пол)
 - stress (уровень стресса - высокий, средний, низкий)
 - t1 (тест производительности, 1 измерение)
 - t2 (тест производительности, 2 измерение)
 
Ваша первая задача будет заключаться в сравнении средних значений производительности до и после участия в программе. Поскольку данные количественные, для этой цели лучше всего подойдет t-критерий для зависимых выборок.

Напомним, что для того, чтобы посчитать этот критерий, необходимо сделать следующее:

-   Посчитать различия в значениях между двумя измерениями – $D$
-   Посчитать среднее значение получившихся различий $d_1, d_2, … , d_n$:

$$\bar{d}=\frac{d_1+d_2+...+ d_n}{n}$$ 

- Посчитать значение t-критерия по формуле:

$$t = \frac{\bar{d}}{s/\sqrt{n}},$$ где $s$ - стандартное отклонение различий ($d$), $n$ - объем выборки (количество пар значений, по которым высчитываются $d$)

Далее мы можем вычислить p-значение для абсолютного значения статистики критерия ($|t|$) на основе сведений о количестве степеней свободы ($df$): $df=n−1$.

Итак, изучите данные (для себя самостоятельно), и напишите код, показывающий различия в средней производительности до и после.

```{r t-paired, exercise = TRUE, eval=TRUE}

```

```{r t-paired-hint-1, eval = FALSE}
t.test(data$..., data$..., paired=...)
```


```{r t-paired-solution, include = FALSE}
t.test(data$t1, data$t2, paired=TRUE)
```

```{r t-paired-test, include = FALSE}
t.test(data$t1, data$t2, paired=TRUE)
```
```{r t-paired-code-check}
grade_code()
```

```{r t-paired-essay}
question_text(
    "Что Вы можете сказать по поводу результатов - дайте небольшой комментарий:",
    answer(" ", correct = TRUE),
    allow_retry = TRUE,
    try_again = "Edit Answer",
    incorrect = NULL)
```

### Упражнение 2

Переведите данные в "длинный формат" и создайте визуализацию с помощью функции `ggwithinstats()`. У Вас должно получиться следующее:

```{r ttest-vis-example, echo=FALSE}
data<-data |> 
  pivot_longer(cols=t1:t2, 
names_to="time",
values_to = "performance")
ggwithinstats(
  data = data,
  x = time,
  y = performance
)
```

```{r ttest-vis, exercise = TRUE, eval=TRUE}

```


```{r ttest-vis-hint-1, eval = FALSE}
data_long<-data |> 
  pivot_longer(cols=...:t2, 
names_to="...",
values_to = "performance")
ggwithinstats(
  data = ...,
  x = time,
  y = ...
)
```


```{r ttest-vis-solution, include = FALSE}
data_long<-data |> 
  pivot_longer(cols=t1:t2, 
names_to="time",
values_to = "performance")
ggwithinstats(
  data = data_long,
  x = time,
  y = performance
)
```

```{r ttest-vis-code-check}
grade_code()
```



### Упражнение 3

У нас есть данные по поводу самооценки некоторой группы лиц, измеренной три раза по время похудения, и мы хотим проверить, действительно ли следование диете приводит к изменению представлений о себе и повышает самооценку.

Поскольку групп больше двух, для сравнения средних значений нам требуется ANOVA с повторными измерениями.

Дисперсионный анализ с повторными измерениями расширяет идею t-критерия для зависимых выборок на случай, когда количество сравниваемых групп превышает две.

Вычисления сходны с теми, что мы рассматривали на примере «обычной» однофакторной ANOVA.

Различия будут заключаться в том, как мы будем подсчитывать среднюю межгрупповую сумму квадратов отклонений, для повторных измерений у в качестве группирующей переменной будет использоваться фактор времени:

$$F=\frac{MS_{time}}{MS_{error}}$$

или фактор условия: $$F=\frac{MS_{condition}}{MS_{error}}$$

Например, мы хотим сравнить значения, соответствующие различным временным отрезкам.

Тогда разница группового (внутри отдельных временных диапазонов) и общего среднего составит:

$$S_{time}=SS_b=\sum_{i=1}^{k}(\bar{x_i}-\bar{x})^2$$ Разница индивидуальных значений и групповых средних:

$$SSw=\sum_{1}(x_{i1}-\bar{x_1})^2+\sum_{2}(x_{i2}-\bar{x_2})^2+ ... + \sum_{k}(x_{ik}-\bar{x_k})^2$$ Разница средних по всем измерениям и общего среднего, k – количество уровней:

$$SS_{subjects}=k*\sum(\bar{x_i}-\bar{x})^2$$

Сумма квадратов ошибки:

$$SS_{error}=SS_w-SS_{subjects}$$

Тогда средний квадрат отклонений по времени будет:

$$MS_{time}=\frac{SS_{time}}{k-1}$$ Соответственно, будет определяться и средняя ошибка:

$$MS_{error}=\frac{SS_{error}}{(n-1)(k-1)}$$

Посчитайте различия в самооценке. Учитывайте, что данные в "широком" формате, а нам нужен "длинный". 

```{r anova, exercise = TRUE, eval=TRUE}
data("selfesteem")
data_long<-selfesteem |> 
    pivot_longer(cols=t1:t3, 
                 names_to="time",
                 values_to = "selfesteem")
res<-aov(selfesteem~time+Error(id/time), data=data_long)
summary(res)
```

```{r anova-test, include = FALSE}
data("selfesteem")
data_long<-selfesteem |> 
    pivot_longer(cols=t1:t3, 
                 names_to="time",
                 values_to = "selfesteem")
res<-aov(selfesteem~time+Error(id/time), data=data_long)
summary(res)
```

```{r anova-hint-1, eval = FALSE}
data("selfesteem")
data_long<-selfesteem |> 
    pivot_longer(cols=t1:t3, 
                 names_to="...",
                 values_to = "...")
res<-aov(...~time+Error(id/time), data=...)
summary(...)
```


```{r anova-solution, include = FALSE}
data("selfesteem")
data_long<-selfesteem |> 
    pivot_longer(cols=t1:t3, 
                 names_to="time",
                 values_to = "selfesteem")
res<-aov(selfesteem~time+Error(id/time), data=data_long)
summary(res)
```

```{r anova-code-check}
grade_code()
```

```{r anova-essay}
question_text(
    "Что Вы можете сказать по поводу результатов - дайте небольшой комментарий:",
    answer(" ", correct = TRUE),
    allow_retry = TRUE,
    try_again = "Edit Answer",
    incorrect = NULL)
```

### Упражнение 4

Поскольку у нас три группы, и F-критерий показывает только общую оценку различий, и чтобы понять, какие именно группы (в данном случае - временные периоды, когда проводилась оценка) отличаются, нам нужно было бы сделать парные сравнения с поправкой (Бонферрони, Холма и др.).

Вместо этого, сделайте визуализацию с помощью функции `ggwithinstats()`, внутри уже содержатся все необходимые статистические рассчеты.

Нам снова нужны "длинные данные". Чтобы их получить, скопируйте код из предыдущего упражнения.

Должно получиться вот так:

```{r, echo=FALSE}
data("selfesteem")
data_long<-selfesteem |> 
    pivot_longer(cols=t1:t3, 
                 names_to="time",
                 values_to = "selfesteem")
ggwithinstats(
  data = data_long,
  x = time,
  y = selfesteem
)
```

```{r anova-viz, exercise = TRUE, eval=TRUE}

```

```{r anova-viz-test, include = FALSE}
data("selfesteem")
data_long<-selfesteem |> 
    pivot_longer(cols=t1:t3, 
                 names_to="time",
                 values_to = "selfesteem")
ggwithinstats(
  data = data_long,
  x = time,
  y = selfesteem
)
```

```{r anova-viz-hint-1, eval = FALSE}
data("selfesteem")
data_long<-selfesteem |> 
    pivot_longer(cols=t1:t3, 
                 names_to="time",
                 values_to = "selfesteem")
ggwithinstats(
  data = data_long,
  x = time,
  y = selfesteem
)
```


```{r anova-viz-solution, include = FALSE}
data("selfesteem")
data_long<-selfesteem |> 
    pivot_longer(cols=t1:t3, 
                 names_to="time",
                 values_to = "selfesteem")
ggwithinstats(
  data = data_long,
  x = time,
  y = selfesteem
)
```

```{r anova-viz-code-check}
grade_code()
```

```{r, holm, warning=FALSE}
question_radio("Какая поправка используется в визуализации для множественных сравнений средних значений?",
  answer("Холма", correct = TRUE),
  answer("Бонферрони"),
  answer("Бенджамини-Хохберга"),
allow_retry = TRUE
)
```

```{r mu-1-essay}
question_text(
    "Что Вы можете сказать по поводу результатов - дайте небольшой комментарий:",
    answer(" ", correct = TRUE),
    allow_retry = TRUE,
    try_again = "Edit Answer",
    incorrect = NULL)
```

### Упражнение 5

Проведите анализ производительности по набору `data` из упражнения 1, но с помощью непараметрического аналога - теста Вилкоксона для повторных измерений. Используйте двухстороннюю альтернативу.

```{r wilcox, exercise = TRUE, eval=TRUE}

```


```{r wilcox-hint-1, eval = FALSE}
wilcox.test(data$..., ...$t2, paired = ..., alternative = "two.sided")
```


```{r wilcox-solution, include = FALSE}
wilcox.test(data$t1, data$t2, paired = TRUE, alternative = "two.sided")
```

```{r wilcox-code-check}
grade_code()
```

```{r wilcox-essay}
question_text(
    "Что Вы можете сказать по поводу результатов - дайте небольшой комментарий:",
    answer(" ", correct = TRUE),
    allow_retry = TRUE,
    try_again = "Edit Answer",
    incorrect = NULL)
```
### Упражнение 6


Проведите анализ самооценки (`data_long`) с помощью теста Фридмана. Сравните результаты с дисперсионным анализом для повторных измерений (упражнение 3). Какие выводы можно сделать?

```{r fried, exercise = TRUE, eval=TRUE}

```

```{r fried-test, include = FALSE}
res<-data_long |> 
  friedman_test(selfesteem ~ time |id)
res
```

```{r fried-hint-1, eval = FALSE}
res<-data_long |> 
  friedman_test(selfesteem ~ time |id)
res
```


```{r fried-solution, include = FALSE}
res<-data_long |> 
  friedman_test(selfesteem ~ time |id)
res
```

```{r fried-code-check}
grade_code()
```

```{r js-aov-essay}
question_text(
    "Отличаются ли результаты сравнения самооценки по ANOVA и тесту Фридмана?",
    answer(" ", correct = TRUE),
    allow_retry = TRUE,
    try_again = "Edit Answer",
    incorrect = NULL)
```

### Упражнение 7

На основе результатов исследования гражданского общества (`civil_society`), проанализируйте взаимосвязь между местом проживания (`place`) и опытом взаимодействия с НКО (`NKO_link`). Можно предположить, что в городах количество НКО больше и у жителей есть больше шансов взаимодействовать с ними? Так ли это? Докажите.

1. Сделайте таблицу сопряженности на базовом R `prop.table(table(civil_society$place, civil_society$NKO_link), margin=1)*100`
2. Посчитайте статистику хи-квадрат для этих переменных: `chisq.test(civil_society$place, civil_society$NKO_link)`

```{r chi-setup, include=FALSE}
civil_society<-haven::read_sav("data/civil_society.sav")
civil_society<-haven::as_factor(civil_society)
```


```{r chi, exercise = TRUE, eval=TRUE}

```

```{r chi-hint-1, eval = FALSE}
prop.table(table(civil_society$place, civil_society$NKO_link), margin = 1)*100
chisq.test(civil_society$place, civil_society$NKO_link)
```

```{r chi-solution, include = FALSE}
prop.table(table(civil_society$place, civil_society$NKO_link), margin = 1)*100
chisq.test(civil_society$place, civil_society$NKO_link)
```

```{r chi-code-check}
grade_code()
```

```{r chi-essay}
question_text(
    "Что Вы можете сказать по поводу результатов - дайте небольшой комментарий:",
    answer(" ", correct = TRUE),
    allow_retry = TRUE,
    try_again = "Edit Answer",
    incorrect = NULL)
```

### Упражнение 8

Используя возможности библиотеки `ggstatsplot` и функции `ggbarstats` создайте график, показывающий различия во взаимодействии с НКО у жителей различных поселений. 

У Вас должно получиться следующее:

```{r echo=FALSE}
ggbarstats(civil_society, x = NKO_link, y = place)

```


```{r ggbar, exercise = TRUE, eval=TRUE}

```

```{r ggbar-hint-1, eval = FALSE}
ggbarstats(civil_society, x = NKO_link, y = place)
```


```{r ggbar-solution, include = FALSE}
ggbarstats(civil_society, x = NKO_link, y = place)
```

```{r ggbar-code-check}
grade_code()
```


```{r download-answers, child ="child_documents/download_answers.Rmd"}
```
