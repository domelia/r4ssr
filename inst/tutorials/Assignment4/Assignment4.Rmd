---
title: "Основы модификации данных"
output: 
  learnr::tutorial:
    language: ru.json
runtime: shiny_prerendered
---

```{r setup-locale, include=FALSE}
Sys.setenv(LANGUAGE = "ru_RU.UTF-8")
Sys.setlocale("LC_ALL", "Russian")
```

```{r setup, include=FALSE}
library(learnr)
library(gradethis)
library(tutorial.helpers)
library(tidyverse)

knitr::opts_chunk$set(echo=FALSE)
```


```{r info-section, child = "child_documents/info_section.Rmd"}
```

## План действий

В рамках данной практической работы мы научимся проводить трансформации с данными, включающие следующие действия:

* Создание новых переменных с помощью функции `mutate()`
* Комбинация `mutate()` с другими функциями для перекодирования одних значений переменной в другие, учета разных условий
* Подсчет простых описательных статистик через функцию `summarise()`
* Переход от «линейного», «горизонтального» кода к написанию более сложных цепочек действий через оператор конвейера (pipe)
* Подсчет количества наблюдений с помощью `n()` и `count()`
* Подсчет агрегированных значений с помощью `group_by()` и `summarise()`

Основной теоретический материал для чтения содержится в разделе 7 электронного пособия [_R в социологических исследованиях_](https://domelia.github.io/R-book/TidyverseTransformations.html).

Материалы и упражнения, представленные в практической работе, вдохновлены классическим учебником [_R for Data Science_](http://r4ds.had.co.nz/) и материалами, предоставленными разработчиками пакета [learnr](https://rstudio.github.io/learnr/).


### Установка пакетов и загрузка данных

Чтобы отработать навыки преобразования данных мы будем продолжать использовать набор микроданных комплексного наблюдения за условиями жизни Росстата, с которым мы познакомились на прошлом практическом занятии.Это сокращенный набор в формате SPSS, содержащий только некоторые переменные. 

Вы можете [скачать данные](https://github.com/domelia/r4ssr/blob/master/inst/tutorials/Assignment3/data/IND_OSN.sav) и поэкспериментировать с ними и теми функциями, которые мы будем проходить, самостоятельно.

Чтобы загрузить эти данные в RStudio, можно воспользоваться следующим кодом:
```{r echo=TRUE, eval=FALSE}
haven::read_sav("IND_OSN.sav")
```


```{r data, echo=FALSE}
df<-haven::read_sav("data/IND_OSN.sav")
df$H01_01 <-sjlabelled::as_label(df$H01_01)
df$I07_01<-sjlabelled::as_label(df$I07_01)
df$FED_OKR<-sjlabelled::as_label(df$FED_OKR)
df$SEKT<-sjlabelled::as_label(df$SEKT)


```

```{r eval = FALSE}
library(tidyverse) #  сразу загружает dplyr, ggplot2, и другие пакеты.
```

## Создание новых переменных с mutate()

Чаще всего новые переменные создаются на основе той информации, которая уже содержится в датасете, и функция `mutate()` помогает нам это сделать. Так как  `mutate()` всегда создает новую переменную и копирует ее в конец датасета. 
Используем  более узкий датасет, в котором будут содержаться только те переменные, которые мы будем использовать в упражнениях, чтобы нам было видно, что происходит с набором данных. 

Переменные сокращенного набора:

- H01_01 - Пол
- H01_02 - Возраст (количество лет)
- DOX_mean - Средний доход домохозяйства, в месяц, рублей
- R_2_0 - Состав домохозяйства (количество человек)
- I07_01 - Какое образование Вы имеете?
- I07_04 - Сколько лет суммарно Вы затратили на обучение, чтобы достичь нынешнего уровня образования?
- PM_DX_SRED_MES - Граница бедности на члена домохозяйства, месяц (руб.)
- I05_18 - Сколько часов в неделю Вы обычно работаете (или работали) на своей основной работе, включая сверхурочную работу, если таковая имеется?

```{r mutate1-setup}
df_sml<- select(df, 
                H01_01, 
                H01_02, 
                DOX_mean,
                R_2_0, 
                I07_01,
                I07_04,
                I05_18)

```

Код ниже создает две новых переменных:

- переменную 'DOH_NA_CHEL' путем деления средних доходов домохозяйства на количество человек, которые в нем проживают, и 
- количество лет обучения в год - частное от деления общего количества лет обучения на возраст опрошенного. 

Функция `mutate()` возвращает новый датафрейм, который содержит новые переменные, присоединенные к исходным данным.

```{r mutate1, exercise = TRUE, exercise.eval = FALSE}
mutate(df_sml,
  DOH_NA_CHEL = DOX_mean / R_2_0,
 OBUCH_V_GOD = I07_04 / H01_02
  
)
```

Отметим, что через `mutate()` мы можем сразу создать несколько переменных, а также последовательно создавать одни переменные на основе других в том же блоке кода. 
Например, мы можем посчитать средний доход на одного члена домохозяйства и сразу же использовать эту новую переменную для вычисления разницы со средним значением по всей выборочной совокупности:

```{r echo = FALSE}
df_sml<- select(df, 
                H01_01, 
                H01_02, 
                DOX_mean,
                R_2_0, 
                I07_01,
                I07_04, 
                PM_DX_SRED_MES)
```


```{r echo=TRUE}
mutate(df_sml,
       DOH_NA_CHEL = DOX_mean / R_2_0,
       RAZ_DOH=DOH_NA_CHEL-mean(DOH_NA_CHEL, na.rm=TRUE)
)
```

### transmute()

`mutate()` всегда будет прикреплять переменные в конце копии оригинальных данных. Если мы хотим оставить только новые переменные, нам нужна функция `transmute()`. В коде ниже замените `mutate()` на `transmute()` и посмотрите, как будут отличаться результаты.

```{r transmute-setup}
df_sml<- select(df, 
                H01_01, 
                H01_02, 
                DOX_mean,
                R_2_0, 
                I07_01,
                I07_04, 
                PM_DX_SRED_MES)
```
```{r transmute, exercise = TRUE, exercise.eval = FALSE}
mutate(df_sml,
       OBUCH_V_GOD = I07_04 / H01_02,
       DOH_NA_CHEL = DOX_mean / R_2_0,
       RAZ_DOH=DOH_NA_CHEL-mean(DOH_NA_CHEL, na.rm=TRUE)
)
```

```{r transmute-solution}
transmute(df_sml,
       OBUCH_V_GOD = I07_04 / H01_02,
       DOH_NA_CHEL = DOX_mean / R_2_0,
       RAZ_DOH=DOH_NA_CHEL-mean(DOH_NA_CHEL, na.rm=TRUE)
)
```

```{r transmute-code-check}
grade_code()
```

### Полезные функции для mutate

Внутри `mutate()` мы можем использвать другие функции, так как она является  **векторизованной**. Векторизованная функция берет на вход вектор значений и возвращает вектор с таким же количеством значений в качестве вывода.

Какие функции будут особенно полезны с `mutate()`?

*   **Арифметические операторы**: `+`, `-`, `*`, `/`, `^`. Все они являются векторизованными и используют так называемые *правила рециркуляции*. Если один параметр будет короче, чем другой, он будет автоматически повторяться, пока длина векторов не сравняется. Это наиболее ярко проявляется, когда мы используем в формуле какое-то одно число: `time / 60`, `hours * 60 + minute` и т.д.

*   **Логические сравнения**, `<`, `<=`, `>`, `>=`, `!=`, с которыми мы уже хорошо знакомы. Если мы используем сложное сочетание команд с логическими операциями, полезным будет сохранить промежуточные значения.

*   **Ранжирование (Ranking)**: есть несколько ранжирующих функций, позволяющих упорядочить значения, таких, например, как  `min_rank()`. Эта функция делает самое часто требуемое действие - присваивает ранги на основе значений (1-е, 2-е, 3-е, 4-е место и т.д.). Обычно, наименьшему значению присваиваются меньшие ранги, если нам нужно обратное, то мы можем упорядочить значения по убыванию с помощью `desc(x)`.

```{r echo=TRUE}
y <- c(1, 2, 2, NA, 3, 4)
min_rank(y)
min_rank(desc(y))
```

Если `min_rank()` нам не подходит, другие функции могут быть полезными:   `row_number()`, `dense_rank()`, `percent_rank()`, `cume_dist()`, `ntile()`.  Изучите их действие самостоятельно.

```{r echo=TRUE}
row_number(y)
dense_rank(y)
percent_rank(y)
cume_dist(y)
```


```{r, eval = FALSE, echo = FALSE}
df_sml <- mutate(df_sml,
       OBUCH_V_GOD = I07_04 / H01_02,
       DOH_NA_CHEL = DOX_mean / R_2_0,
       RAZ_DOH=DOH_NA_CHEL-mean(DOH_NA_CHEL, na.rm=TRUE))
```

### Упражнение 1

Посчитайте какой средний доход имеет домохозяйство в год? Назовите новую переменную `DOH_GOD`. Обращайтесь к полному набору данных `df`.

```{r mutateex1, exercise = TRUE}

```
```{r mutateex1-solution}
mutate(df, DOH_GOD = DOX_mean * 12)
```

<div id="mutateex1-hint">
**Подсказка:** Все просто: нужно просто умножить среднемесячный доход на 12 .
</div>

```{r mutateex1-code-check}
grade_code()
```

### Упражнение 2

Сравните средний доход домохозяйства `DOX_mean` с границей бедности `PM_DX_SRED` с помощью  выражения `DOX_mean - PM_DX_SRED_MES`. Что мы можем сказать о получившихся значениях? Сравните эти значения с теми, что мы получили выше, когда сравнивали средний доход домохозяйства со средним доходом по всему массиву?

```{r mutateex2-setup}
df_sml<- select(df, 
                H01_01, 
                H01_02, 
                DOX_mean,
                R_2_0, 
                I07_01,
                I07_04, 
                PM_DX_SRED_MES)
df_sml <- df_sml |> 
  mutate(df_sml,
       OBUCH_V_GOD = I07_04 / H01_02,
       DOH_NA_CHEL = DOX_mean / R_2_0,
       RAZ_DOH=DOH_NA_CHEL-mean(DOH_NA_CHEL, na.rm=TRUE))
```
```{r mutateex2, exercise = TRUE}
# df_sml <- mutate(df_sml, RAZN_PM = _____________)
# df_razn <- select(df_sml, RAZ_DOH, RAZN_SRED)
```
```{r mutateex2-solution}
df_sml <- mutate(df_sml, RAZN_PM = DOX_mean - PM_DX_SRED_MES)
select(df_sml, RAZN_PM, RAZ_DOH)
```

```{r mutateex2-code-check}
grade_code()
```

### Упражнение 3

Найдите 10 наблюдений с самым большим количеством часов работы в неделю `I05_18`?

```{r mutateex3, exercise = TRUE}

```
```{r mutateex3-solution}
arrange(df, desc(I05_18))
```

```{r q1}
question_numeric(
  "Каково максимальное количество часов работы в неделю?",
answer(83, message = "Почти! Проверьте max(df$I05_18)."),
  answer(84, correct = TRUE, message = "Верно!"),
  allow_retry = TRUE,
  min = 0,
  max = 100,
  step = 1
)
```
  
<div id="mutateex3-hint">
**Hint:** Как только вы отсортируете по убыванию, вы увидите 10 строк с максимальными значениями
</div>

```{r mutateex3-check}
"Вы хорошо поработали!"
```

### Упражнение 4

Какой результат у выражения `1:3 + 1:10`? Почему?

```{r mutateex4, exercise = TRUE}

```
```{r mutateex4-solution}
1:3 + 1:10
```

<div id="mutateex4-hint">
**Hint:** Remember R's recycling rules.
</div>

```{r mutateex4-check}
"Здорово! R повторяет 1:3 три раза, чтобы создать вектор длины, достаточной для сложения с вектором 1:10. Поскольку длины векторов не совпадают, R также возвращает ошибку."
```

### if_else()

Вместе с `mutate()` часто используется конструкция `if_else()`, чтобы создать новые переменные.

Например, давайте создадим переменную количества лет обучения OBUCH_BIN: если оно больше, чем в среднем по всему набору, то будем присваивать 1, а если меньше, то 0.
```{r echo=TRUE}
df_sml |> 
  mutate(OBUCH_BIN = if_else(I07_04 >= mean(I07_04, na.rm=T), 1, 0))
```

### Упражнение 5

Создайте новую переменную `SOSTAV_BIN`, в которой разделите все домохозяйства по составу R_2_0 на две группы - до 2 человек (код 1) и всех остальных (код - 0).

```{r ex5-setup}
df_sml<- select(df, 
                H01_01, 
                H01_02, 
                DOX_mean,
                R_2_0, 
                I07_01,
                I07_04, 
                PM_DX_SRED_MES)
```
```{r ex5, exercise = TRUE}

```

```{r ex5-solution}
df_sml |> 
  mutate(SOSTAV_BIN = if_else(R_2_0 >= 2, 1, 0))
```

<div id="ex5-hint">
**Подсказка:** задача проще, чем в примере, нужно просто наменить имена переменных и задать новое условие.
</div>

```{r ex5-code-check}
grade_code()
```


### `case_when()`

Когда условий несколько, можно использовать вложенные конструкции `if_else()`, но это не очень удобно. Вместо это лучше использовать специальную функцию `case_when()`, которая как раз предназначена для таких случаев.

Допустим, нам нужно создать переменную, которая бы разбивала все домохозяйства по уровню доходов на тех, у кого средний доход DEN_NA_DUSHU меньше, чем граница бедности PM_DX_SRED_MES, тех, у кого этот доход находится в диапазоне двух границ, и тех, у кого доход выше, чем две суммы, означающие границы бедности.

```{r echo=TRUE}
df |> 
  mutate(DOH_GROUPS=  case_when(
  DEN_NA_DUSHU < PM_DX_SRED_MES ~ "меньше уровня бедности",
  between(DEN_NA_DUSHU, PM_DX_SRED_MES, PM_DX_SRED_MES*2) ~ "от одной до двух границ",
  DEN_NA_DUSHU > PM_DX_SRED_MES*2 ~ "больше двух границ"), .keep="used") # .keep="used" похожа на transmute() - оставляет только используемые переменные
```

### Упражнение 6

Сделайте аналогичную перекодировку по переменной количества лет обучения `I07_04`, задав в качестве границ - 11 и 15 лет обучения. Новую переменную задайте EDUC_GROUPS:

```{r ex6, exercise = TRUE}

```

```{r ex6-solution}
df |> 
  mutate(EDUC_GROUPS=  case_when(
  I07_04 < 11 ~ "меньше 11 лет",
  between(I07_04, 11, 15) ~ "11-15 лет",
  I07_04 > 15 ~ "больше 15 лет"), .keep="used")
```

```{r ex6-code-check}
grade_code()
```

## Суммация данных по группам с помощью summarise()


Функция `summarise()` "схлопывает" датафрейм до единственной строки (или нескольких строк) с агрегированными значениями. Мы сами выбираем, сколько показателей будет выведено и как они будут рассчитаны, что совсем не совпадает с подходом готовых пакетов для обработки данных, когда вся информация выводится в готовом виде.

Например, посчитаем среднее количество часов работы в неделю и максимальное количество часов:

```{r summarise, echo=TRUE}
summarise(df, rabota_mean = mean(I05_18, na.rm = TRUE), # na.rm = TRUE - удаляет пропущенные значения
                  rabota_max =  max(I05_18, na.rm = TRUE))
```

Синтаксис `summarise()` очень похож `mutate()`. Так же как и mutate()`, в summarise мы указываем:

1. Имя набора данных
2. Одно или больше имя переменной, которые должны отобразиться в выводе. Для каждого имени столбца мы присваиваем какое-то выражение, преобразующее содержание данной колонки. 

Главное отличие `summarise()` и `mutate()` состоит в отображении результата. Если `mutate()` создает новый столбец с количеством значений равных всей таблице, то `summarise()` - выводит только одно значение.

### group_by()

Хотя само по себе `summarise()` используется достаточно часто, эта функция особенно полезна в сочетании с группировкой `group_by()`. `group_by()` меняет единицу анализа в датафрейме, распределяя наблюдения по отдельным группам и передает `dplyr` инструкции - применять функции отдельно в каждой группе. `group_by()` создает группы на основе объединения наблюдений, имеющих идентичную комбинацию значений для переменной, которую мы переаем в  `group_by()`.

Например, выше мы посчитали с помощью `summarise()`среднее количество часов работы по всему набору данных. Если мы применим тот же самый код для группировки по уровню образования, мы получим среднее количество часов работы для каждого образовательного уровня. Выполните следующий код:

```{r summarise2, exercise = TRUE}
by_educ <- group_by(df, I07_01)
summarise(by_educ, rabota_mean = mean(I05_18, na.rm = TRUE), # na.rm = TRUE - удаляет пропущенные значения
                  rabota_max =  max(I05_18, na.rm = TRUE))
```

```{r summarise2-check}
"Отличная работа!"
```


### Упражнение 7

Как связаны уровень образования и количество лет обучения? Посчитайте среднее и максимальное количество лет обучения для каждой образовательной группы созданного выше сгруппированного набора `by_educ`:

```{r ex7-setup}
by_educ <- group_by(df, I07_01)
```
```{r ex7, exercise = TRUE}

```

```{r ex7-solution}
summarise(by_educ, 
          educ_mean = mean(I07_04, na.rm = TRUE),
          educ_max = max(I07_04, na.rm = TRUE))
```

<div id="ex7-hint">
**Подсказка:** задача аналогична выполненной выше про работу, нужно просто поменять имся переменной.
</div>

```{r ex7-code-check}
grade_code()
```


### Пайплайны (pipes)

То, как мы делали группировку и вычисление максимального среднего значений выше, не очень удобно: мы создали сначала сгруппированный массив, а потом применили к нему какие-то трансформации. 

Мы можем соединить все операции вместе с помощью оператора пайпа `%>%` (это оператор из пакета `magrittr`, он загружается вместе с  `tidyverse`. В последнее время все большее количество пользователей делают выбор в пользу "нативного" оператора `|>` , для которого не нужны никакие пакеты. 

У этого оператора очень интересная история, прочитать об этом можно в пособии.

Итак, как мы могли бы по-другому написать наш код?

```{r echo=TRUE}
df %>%
  filter(!is.na(I07_04))%>%
  group_by(I07_01) %>%
  summarise(educ_mean = mean(I07_04, na.rm = TRUE),
          educ_max = max(I07_04, na.rm = TRUE))

```

Мы сразу сделали несколько трансформаций: отфильтровали пропущенные значения по переменной образования, сделали группировку  и посчитали нужные значения.


В дальнейшем мы будем очень часто использовать такие пайплайны, посколько они делают код более читабельным. 

Кроме того, оператор pipe  - это одна из характерных особенностей экосистемы tidyverse: все пакеты, которые туда входят, содержат функции, которые приспособлены для того, чтобы использовать такие контейнеры с кодом. Единственным исключением является пакет для создания визуализаций ggplot2: поскольку он был написан до появления оператора pipe, он там не используется, а следующее поколение ggplot2, ggvis, где можно использовать данный оператор, пока не явлется таким распространенным


### Группировка по нескольким переменным

Когда мы делаем группировку по нескольким переменным, каждое посчитанное значение соответствует своему уровню, что позволяет сделать более глубокие и детализированные сравнения. Давайте попробуем сделать группировку по полу и образованию и посмотреть среднее количество часов работы в неделю.

```{r unwrap, exercise = TRUE}
df |> 
  filter(I07_01!="") |> 
  group_by(I07_01, H01_01) |> 
  summarise(rabota_mean =  mean(I05_18, na.rm = TRUE)) 

```

Такая прогрессивная группировка хорошо работает с временными переменными (годами и месяцами), но нужно быть осторожными: общая сумма по показателю будет равна сумме отдельных сумм по группам, но вот другие меры, например, медиана, могут не совпадать.



### Упражнение 8

Сделайте группировку по федеральному округу `FED_OKR` и сектору экономики `SEKT`  и посчитайте средние доходы домохозяйств в месяц `DOX_mean`, сохранив значения в переменной `DOH_mean`:
```{r ex8, exercise = TRUE}

```

<div id="ex8-hint">
**Подсказка:** Нужно посчитать среднее средних, то есть использовать функцию `mean()`.
</div>

```{r ex8-solution}
df |> 
  filter(SEKT!="") |> 
  group_by(FED_OKR, SEKT) |> 
  summarise(DOH_mean=mean(DOX_mean, na.rm=TRUE))
```

```{r ex8-code-check}
grade_code()
```


## Частоты

### n()

Когда мы имеем дело с агрегированными значениями, мы можем использовать частоты (`n()`), или количество непропущенных значений(`sum(!is.na(x))`) для того, чтобы проконтролировать, что мы не строим свои умозаключения на каком-то очень маленьком количестве данных. Например, давайте посмотрим еще раз на сренее количество часов работы в неделю:

```{r n_count, exercise=TRUE, exercise.eval=FALSE}
df %>%
  group_by(FED_OKR) %>%
  summarise(
    Mean = mean(I05_18, na.rm=TRUE),
    Count=n()
  )
```

Нельзя сказать, что данные по округам сбалансированы, что, вероятно, связано с объективным количеством населения, проживающего в них.

### count()

Есть и специальная функция, которая может сразу дать нужную информацию:

```{r echo=TRUE}
df |> 
  count(FED_OKR)
```


### Упражнение 9

Напишите код, который бы показывал аналогичные результаты, но не через `df |>  count(FED_OKR)` (то есть работайте без использования `count()`).

```{r ex9, exercise = TRUE}

```
```{r ex9-solution}
df |> 
  group_by(FED_OKR) |> 
  summarise(n = n())

```

<div id="ex9-hint">
**Подсказка:** Вам лучше воспользоваться следующими функциями: `group_by()`, `summarise()`, `n()`.
</div>

```{r ex9-code-check}
grade_code()
```



```{r download-answers, child ="child_documents/download_answers.Rmd"}
```
